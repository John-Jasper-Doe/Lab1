dist: xenial
language: cpp
before_script:
- sudo apt update
- sudo apt install libboost-test-dev -y
script:
- cmake .
- cmake --build .
- cmake --build . --target test
- cmake --build . --target package
deploy:
  provider: script
  skip_cleanup: true
  script:
  - curl -T helloworld-0.0.$TRAVIS_BUILD_NUMBER-Linux.deb -uJohn-Jasper-Doe:$BINTRAY_API_KEY "https://api.bintray.com/john-jasper-doe/otus-cpp/Lab_1/$TRAVIS_BUILD_NUMBER/helloworld-0.0.$TRAVIS_BUILD_NUMBER-Linux.deb;deb_distribution=xenial;deb_component=main;deb_architecture=amd64;publish=1"

